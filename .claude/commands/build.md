# Build Vaultic

Build the project in release mode and verify it works.

```bash
cargo build --release 2>&1
./target/release/vaultic --version
```

Report build status and any warnings that should be addressed.
